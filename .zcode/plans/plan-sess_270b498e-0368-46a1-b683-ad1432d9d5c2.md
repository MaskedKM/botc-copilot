# 实施计划：#134 首夜录入效率（合并 #131）

官方规则已核对：声明（白天公开陈述）与信息（按真实角色夜间获得）本就独立，解耦无规则冲突；快捷菜单/复盘/sheet 统一均为 UX/架构，迁移须保住两处私密语义（Imp 爪牙名单仅 7+ 我=恶魔、myRole 私密驱动）。一个 PR，5 commit，关闭 #134 + #131。

## Commit 1 — #131 统一 MyInfoSheet/PlayerDetailSheet 入口
- `game_board_page.dart:124`「我的信息」按钮：改为 `_openMyInfo(context, ref, game)` —— 读 `game.myPlayerId`：已设 → 从 `players` 取本人 → `PlayerDetailSheet.show`；未设 → `MyInfoSheet.show`（仅 onboarding 选座）。
- `my_info_sheet.dart`：剥离为纯 onboarding（保留首夜选座 UI）；`_changeSeatDialog`、`_MyMinionsSection` 迁出。
- `player_detail_sheet.dart` isMe 分支追加：换座按钮（仅 ongoing，迁入 `_changeSeatDialog`）+ 爪牙名单区（`imp && playerCount>=7` 迁入 `_MyMinionsSection`，私密提示保留）。

## Commit 2 — 声明+信息解耦（核心痛点）
`player_detail_sheet.dart`：
- 信息区可见性改由 `infoRole = isMe ? myRole : displayRole`（草稿感知）驱动 → 选 chip 即刻出现对应信息录入，免去「保存→关→重开」。
- 新增 `_commitDraftClaim()`：非己 && `_roleTouched && _draftRole!=null && != saved` → `ensureCurrentDayRecord` + `claimRole` + `setState(_claimAutoCommitted=true)`。
- `_InfoInputSection.onSubmit` 前 `await onEnsureClaim()`（传入 `_commitDraftClaim`），**杜绝孤儿信息记录**（保住 `_RecordedInfoSection` 按角色分组正确性）。
- `_isDirty`/`_save` 角色块加 `!_claimAutoCommitted` 守卫，避免 pre-refresh 窗口重复插声明；`displayRole` 维持 `_draftRole` 防闪烁。

## Commit 3 — 长按快捷菜单（UI-STYLE §6.1 兑现）
`game_board_page.dart`：`onPlayerLongPress` → 新 `_showQuickActions`（底部 sheet，3 项）：
- 标记死亡/复活 → 复用现有 `_quickToggleDead`（含 #136 传承检测）
- 信任度（显示当前级，tap 轮转 好→中→疑→未知→好，直写 `setTrustLevel` + SnackBar）
- 快速备注 → dialog TextField → `addNote`

## Commit 4 — 已结束复盘（只读）
- `home_page.dart:102`：onTap 始终 push（去 `ongoing?` 守卫）。
- `game_board_page.dart`：onPlayerLongPress 保持禁用、推进保持禁用、结束横幅保留；onPlayerLongPress 解禁 sheet 进入（注意仅解禁查看路径）。
- `player_detail_sheet.dart`：自查 `gameByIdProvider`，`status!=ongoing` → `readOnly=true`。只读态：禁所有编辑控件（chip/switch/输入/能力/保存）、隐藏"下一位"、保留全部数据展示（声明/信息/备注/信任/爪牙名单）。两处调用点自动受益，无需传参。

## Commit 5 — 保存并下一位（首夜队列加速器）
- 提取纯函数 `nextUnclaimedPlayer(players, claimedPlayerIds, fromSeat, myPlayerId) → Player?`（按座位序从当前座之后环绕，排除己方与已声明者；全声明则 null）—— 独立可单测。
- `player_detail_sheet.dart`：`show` 返回 `Future<Player?>`；新增 `_saveAndNext`（提取 `_commitChanges()` 复用，不重复 `_save` 的 pop）→ `Navigator.pop(context, next)`；`_save` → `pop(null)`；丢弃/返回 → `pop(null)`。
- "保存并下一位"按钮仅在 `enableChain && !isMe && !readOnly` 显示（无下一位时禁用）；`enableChain` 默认 false，game_board seat-tap 传 true。
- `game_board_page.dart` seat-tap：`_openDetailChain` 循环 `while(cur!=null) cur = await show(cur)`（await 保证上一个 sheet 动画结束再开下一个，无堆叠/闪烁）。

## 测试（每项配套）
- `next_unclaimed_player_test.dart`（新）：纯函数单测 —— 跳过己方/已声明、环绕、全声明返回 null。
- `player_detail_sheet_test.dart`（widget，Fake 仓库）：草稿角色后提交信息 → claimRole 先于 declareInfo 调用（解耦）；finished-game status → 无保存/下一位按钮（只读）；isMe imp 7+ → 爪牙名单可见（#131 迁移）。
- `game_board_page_test.dart`（widget）：长按 → 快捷菜单出现，信任轮转写一次；"下一位"循环开两个玩家。
- 现有角色模板测试若覆盖 Spy freeText（#136 已加）保持绿。

## 验证
`flutter analyze` 零 issue；`flutter test` 全绿。PR 关闭 #134 + #131。